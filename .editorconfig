root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

# Preserve Markdown soft line breaks (two trailing spaces)
[*.md]
trim_trailing_whitespace = false
